a C++ program that simulates a simple shopping cart total.
- declare a variable for the running total
- use the basic
assignment operator (=) to set its starting value
- use at least four different compound assignment operators (+=, -=, *=, /=, %=) to update the total as items are added, discounted, or taxed, and use cout to display the total after each update.